package com;
import com.cli.CliRunner;

/**
 * Main entry point for the Music Band Collection application.
 * This class launches the command-line interface that allows users
 * to manage a collection of music bands with various operations.
 */
public class App{
    /**
     * Main method that starts the CLI application.
     *
     * @param args command-line arguments (not used)
     */
    public static void main(String[] args) {
        CliRunner.start();
    }
}
