CREATE TABLE IF NOT EXISTS music_genres (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    genre_name VARCHAR(50) NOT NULL UNIQUE
);

INSERT INTO music_genres (genre_name) VALUES ('PSYCHEDELIC_ROCK');
INSERT INTO music_genres (genre_name) VALUES ('MATH_ROCK');
INSERT INTO music_genres (genre_name) VALUES ('POST_ROCK');

ALTER TABLE music_bands ADD COLUMN IF NOT EXISTS genre_id INTEGER REFERENCES music_genres(id);

UPDATE music_bands SET genre_id = (SELECT id FROM music_genres WHERE genre_name = music_bands.genre);
