package com.model;

public enum MusicGenre { // we have to complete this and make this enum more useful but we are not allowed to add or remove any genres
    PLACEHOLDER,                        // // we can add methods to this enum but we cannot change the existing genres
    PSYCHEDELIC_ROCK,
    MATH_ROCK,
    POST_ROCK;
}
